apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      # Must be on same node as edd-compute to share PVC
      nodeSelector:
        kubernetes.io/hostname: rp1
      containers:
        - name: gateway
          image: eddisonso/ecloud-gateway:latest
          args:
            - -compute-db
            - /data/compute.db
            - -ssh-port
            - "2222"
            - -http-port
            - "8080"
            - -https-port
            - "8443"
            - -log-service
            - log-service:50051
          ports:
            - name: ssh
              containerPort: 2222
              protocol: TCP
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: https
              containerPort: 8443
              protocol: TCP
          volumeMounts:
            - name: compute-data
              mountPath: /data
              readOnly: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 2222
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 2222
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: compute-data
          persistentVolumeClaim:
            claimName: edd-compute-data
            readOnly: true
---
apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app: gateway
  ports:
    - name: ssh
      port: 22
      targetPort: 2222
      protocol: TCP
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
    - name: https
      port: 443
      targetPort: 8443
      protocol: TCP
